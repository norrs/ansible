location = /sso-logout {
    return 302 /oauth2/sign_out?rd={{ ('https://' ~ pocket_id_hostname ~ '/api/oidc/end-session?id_token_hint={id_token}&post_logout_redirect_uri=' ~ ('https://' ~ oauth2_proxy_hostname ~ '/oauth2/sign_in' | urlencode)) | urlencode }};
}
